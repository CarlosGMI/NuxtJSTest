export default () => ({
    // The invoice that will be created when a Seller wins and the race finishes.
    winnerInvoice: {},
    // A flag to render a loader while the Alegra API is creating the invoice.
    creatingInvoice: false,
});
